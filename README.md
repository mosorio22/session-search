# About

Project to create a UI that allows the end user to filter a “session search” with dynamic
criteria that lets them look at a subset of sessions. Built as part of a React assessment.

## How to run

Make sure you have node installed
Go to directory of project and run "npm start"